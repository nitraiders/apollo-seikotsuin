# 🏰 アポロ整骨院 要塞仕様書 (FORTRESS_MANUAL)

この文書は、アポロ整骨院公式サイト（8ページ構成）の物理構造と運用ルールを記録したものである。

## 1. デプロイメント・システム (Deployment Pipeline)
本サイトは、静的HTML/CSSをベースにしつつ、PHP環境への一括デプロイを容易にするための独自スクリプト群を採用している。

### 🚀 デプロイの流れ
1.  **ソース編集**: `index.html`, `menu.html`, `theme.css` 等を直接編集する。
2.  **ビルド (`node build_deploy_8.js`)**: 
    - 8つのHTMLファイルと `theme.css` をBase64エンコードし、`deploy_all_8.php` を生成する。
3.  **実行**: 生成された `deploy_all_8.php` をサーバーにアップロードして実行することで、すべてのソースが物理展開される。
4.  **バックアップ**: デプロイ実行時、既存ファイルは `backup_[filename]_[timestamp]` として自動的にバックアップされる。

### 🖼️ 画像デプロイ (`deploy_images.php`)
- 画像資産は `scratch/make_img_deployer.js` によって管理される。
- 新しい画像（例：`aqua_tizer.jpg`）を追加した際は、このJSを実行して `deploy_images.php` を更新すること。

---

## 2. 口コミシステム (Review System)
Cloudflare Workers KV を使用した動的な口コミ管理機能を備えている。

### 🛡️ 運用ルール
- **API**: `functions/api/reviews.js` が CRUD 処理を担当。
- **初期化 (Seeding)**: データベース（KV）が空の場合、プログラム内の `INITIAL_REVIEWS`（ホットペッパービューティー由来の実データ）が自動的に投入される。
- **管理**: `/admin.html` から返信および削除（論理削除）が可能。

---

## 3. デザイン・アイデンティティ (Visual Identity)
- **ブランドカラー**:
    - メイン（オレンジ）: `#F39800`
    - サブ（イエロー）: `#FFF100`
- **フォント**: `Noto Sans JP` を標準採用。
- **レイアウトの掟**:
    - ウォーターベッドセクション（`menu.html`）は、左に画像、右に「料金表＋解説文」の2カラム構成を維持すること。

---

## 💎 物理監査記録
2026年5月5日、メニュー改修、デプロイ構文エラー（問題21）の解消、および口コミデータのリアル化を完了。この状態を「安定版」として継承する。
